<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context="com.example.davidemontagnob.myapplication.Database_display_info"
    android:background="@drawable/background_0">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/cardview_shadow_start_color"
        android:minHeight="?attr/actionBarSize"
        app:titleTextColor="@android:color/white"></android.support.v7.widget.Toolbar>

    <ImageView
        android:id="@+id/ic_magnify"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_below="@id/db_txt"
        android:layout_marginStart="9dp"
        android:layout_marginTop="17dp"
        android:src="@drawable/add" />

    <EditText
    android:id="@+id/db_edit_text"
    android:layout_width="321dp"
    android:layout_height="47dp"
        android:layout_marginTop="9dp"
    android:layout_alignParentEnd="true"
    android:layout_toRightOf="@+id/ic_magnify"
    android:layout_below="@+id/db_txt"
    android:background="@drawable/white_border"
    android:hint="Inserisci l'oggetto da comprare"
    android:imeOptions="actionSend"
    android:maxLines="1"
    android:textColor="#000"
    android:textSize="15sp" />

    />



    <TextView
        android:id="@+id/db_txt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_centerHorizontal="true"
        android:fontFamily="cursive"
        android:gravity="center"
        android:text="Siamo sicuri che tu non abbia dimenticato nulla?"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="25sp" />

    <LinearLayout
        android:id="@+id/db_layout_db_list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"

        android:layout_alignParentStart="true"
        android:layout_below="@id/db_edit_text"
        android:layout_gravity="center_vertical"
        android:orientation="vertical">


            />

        <ListView
            android:id="@+id/db_list"
            android:layout_height="match_parent"
            android:layout_width="match_parent"/>
    </LinearLayout>


</RelativeLayout>